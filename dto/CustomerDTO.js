export default class CustomerDTO {
    // Customer kenek hadaddi me fields tika one
    constructor(id, name, contact, nic, address) {
        this.id = id;
        this.name = name;
        this.contact = contact;
        this.nic = nic;
        this.address = address;
    }
}